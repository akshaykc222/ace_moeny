import 'package:flutter/material.dart';

class CreateVpaScreen extends StatelessWidget {
  const CreateVpaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
